# ===========================================================================
# WARNING: DO NOT PUT SECRETS, API KEYS, OR PASSWORDS IN THIS FILE!
# This file is for infrastructure configuration only (ports, paths).
# API keys and credentials are configured through the application UI
# and stored securely in the database.
# ===========================================================================

# Docker Configuration
DATA_PATH=./data
APP_PORT=9876

# Development Configuration
BACKEND_PORT=3333
FRONTEND_PORT=5555
VITE_BACKEND_PORT=3333

# DATABASE_PATH is only for Docker - unused in dev
